{
  "name": "kimi-git-flow",
  "version": "0.2.0",
  "description": "Branch-per-task workflow plugin for Kimi Code.",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/smoke.test.mjs",
    "lint:links": "node --test test/smoke.test.mjs --test-name-pattern=reference"
  }
}
