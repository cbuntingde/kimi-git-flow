# Dependencies / build artifacts
node_modules/
dist/
build/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Kimi Code runtime artifacts
.kimi/
.kimi-code/
.kimi-cache/
# Note: `plugins.local.json` lives in the user's Kimi Code config dir
# (`~/.kimi-code/`), not in this repo, so it does not belong here.
# Add it to `~/.kimi-code/.gitignore` if you manage that dir with git.

# Env files (secrets must never land in the repo)
.env
.env.*
!.env.example

